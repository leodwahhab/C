#include <stdio.h>
#include <stdlib.h>

void main(){
    
    int soma = 0;
    int n = 0;
    char resp[1];


    while(resp != "N"){

        printf("Deseja iniciar/continuar a soma? S/N");
        fgets(resp,1,stdin);

        // printf("Digite um numero: ");
        // scanf("%i",&n);

        // soma = (soma + n);

    }

    
    // printf("\nA soma dos numeros digitados eh: %i",soma);
}